# Project Agent Instructions

Before implementing code changes, provide a short plan and wait for the user to say `oke`, `ok`, `lanjut`, or `continue`.

After approval, implement the plan end-to-end without asking for repeated confirmations unless there is a destructive action, missing credential, or conflicting user change.

Use the project skills in `.agents/skills` when relevant.
